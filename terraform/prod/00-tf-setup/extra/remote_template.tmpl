%{ for remote in remotes ~}
data "terraform_remote_state" "${remote}" {
  backend = "s3"
  config = {
    bucket  = "${bucket_name}"
    region  = "${region}"
    key     = "${remote}.tfstate"
    profile = "${profile}"
  }
}
%{ endfor ~}